# Copy this file to .env and fill in the real values.
# .env is git-ignored and never committed.
#
# No additional secrets are required for the demo-doc demo.
# The llmwiki MCP server runs in local mode without authentication.
#
# If you need to override the WatsonX project ID, do it in agent/config.yaml.
#
# (No variables required for a minimal local demo — keep this file as a
#  placeholder so deploy.sh does not fail the existence check.)
